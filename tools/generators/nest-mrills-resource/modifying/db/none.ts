import { Tree } from 'nx/src/generators/tree'
import { ResourceOptions } from '../../resource.schema'

export async function noneHandler(
  tree: Tree,
  options: ResourceOptions,
): Promise<void> {}
